import {Metadata} from '../call-models/response/metadata';
import {DetailResult} from '../call-models/response/detailResult';

export class BrApiDetailPayloadResponse {
  metadata: Metadata;
  result: DetailResult;
}
